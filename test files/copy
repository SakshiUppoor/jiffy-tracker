<!--
        {% for day in day_list %}
            {{day}}<br>
            {% for d in days %}
                {% if d.name == day %}
                    {% for p in d.periods.all %}
                        <div class="panel">
                            <form method="post">
                                {% csrf_token %}
                                <div class="pname">{{p.name}}</div>
                                <div class="options">
                                    <input type="radio" name="status"> Present<br>
                                    <input type="radio" name="status"> Absent<br>
                                    <input type="radio" name="status"> Cancelled<br>
                                </div> 
                                <input id="reset" class="submit" type="submit" name="{{p.id}}">
                            </form>
                        </div>
                    {% endfor %}
                {% endif %}
            {% endfor %}
        {% endfor %}
        </div>
    </div>
    <div class="head-date"></div>
    {% for l in lectures %}
        {{l.date|date:'d-M-Y'}} {{l.subject.name}} 
        {% with current_date=date|join("-") %} {{current_date}}<br>
        {% if l.date|date:'d-M-Y' == {{current_date}}  %}
            Hi
        {% endif %}
        {% endwith %}
    {% endfor %}-->
    <!--{% for s in sem %}
    {{s.start_date|date:'d-M-Y' }}<br>
    {% endfor %}-->